# 2026-07-10

## 今日摘要
完成了用户态多任务模拟器的两个核心模块：定时器中断模拟和ucontext任务上下文切换，从0开始跑通了基础功能。

## 今日任务

| 项目 | 任务类型 | 任务描述 | 状态 | 备注 |
|------|----------|----------|------|------|
| PCB_TCB | 核心实现 | 定时器中断模拟（setitimer+signal） | 完成 | 使用SIGALRM信号模拟10ms周期中断 |
| PCB_TCB | 核心实现 | ucontext任务上下文切换 | 完成 | 实现任务创建、保存/恢复上下文、任务切换 |
| PCB_TCB | 问题解决 | macOS上ucontext弃用警告 | 完成 | -Wno-deprecated-declarations编译选项 |
| PCB_TCB | 问题解决 | 段错误排查 | 完成 | 发现是swapcontext第一个参数填错、任务函数无循环 |
| PCB_TCB | 项目结构 | 模块拆分（timer/task/kernel） | 完成 | include/ 和 src/ 分离 |

## 今日小结
1. 中断机制：信号可以模拟中断，打断正在执行的任务。
2. 任务切换：ucontext保存CPU寄存器状态，swapcontext完成上下文切换。
3. 任务栈：每个任务要有独立栈，malloc分配堆内存当栈用。
4. 协作调度：任务要主动让出CPU（比如检查调度标志），或等中断触发。
